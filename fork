#include <stdio.h>
#include <unistd.h>
/*
 * main - function nothing
 * return - value 
 */

int main(void)
{
pid_t pid;
printf("before fork i was one\n");
fork();
if (pid == -l)
{
peroor("unsuccessful\n");
return (1);
}
printf("After fork i became two\n");
return (0);
}
